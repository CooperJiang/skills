interface:
  display_name: "Recall"
  short_description: "找回并接手历史会话记忆,支持跨对话、跨工具、打包携带"
  default_prompt: "用 $recall 找回上一段对话、接手上下文继续。"
  brand_color: "#B4472F"

policy:
  allow_implicit_invocation: true
